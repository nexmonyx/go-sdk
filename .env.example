# Nexmonyx Go SDK - Environment Configuration

# ============================================================================
# Integration Testing Configuration
# ============================================================================

# Enable integration tests (disabled by default for safety)
INTEGRATION_TESTS=false

# Integration test mode: "mock" or "dev"
# - "mock": Uses in-memory mock API server (default, recommended)
# - "dev": Uses real Nexmonyx dev API (requires credentials)
INTEGRATION_TEST_MODE=mock

# API URL for integration tests
# For mock mode: uses local docker-compose service
# For dev mode: use actual dev API URL
INTEGRATION_TEST_API_URL=http://localhost:8080

# Authentication token for integration tests
# For mock mode: can be any value (test-token is default)
# For dev mode: use real JWT token from dev environment
INTEGRATION_TEST_AUTH_TOKEN=test-token

# Debug logging for integration tests
INTEGRATION_TEST_DEBUG=false

# Custom timeout for integration tests (duration format: 30s, 5m, etc.)
INTEGRATION_TEST_TIMEOUT=30s

# ============================================================================
# Docker Compose Configuration
# ============================================================================

# Mock API server configuration
API_PORT=8080
API_HOST=0.0.0.0
API_LOG_LEVEL=info

# PostgreSQL configuration (if using database services)
POSTGRES_DB=nexmonyx_test
POSTGRES_USER=test
POSTGRES_PASSWORD=testpass

# Redis configuration (if using cache services)
REDIS_HOST=redis
REDIS_PORT=6379

# ============================================================================
# Development Configuration
# ============================================================================

# Enable verbose logging
DEBUG=false

# GOPROXY for Go module downloads
GOPROXY=https://proxy.golang.org,direct

# ============================================================================
# CI/CD Configuration (GitHub Actions)
# ============================================================================

# Codecov token for coverage reporting (set via GitHub Secrets)
# CODECOV_TOKEN=<your-codecov-token>

# Real API credentials for dev/prod API testing (set via GitHub Secrets)
# NEXMONYX_API_URL=https://dev-api.nexmonyx.com
# NEXMONYX_AUTH_TOKEN=<your-jwt-token>
# NEXMONYX_SERVER_UUID=<server-uuid>
# NEXMONYX_SERVER_SECRET=<server-secret>

# ============================================================================
# Instructions
# ============================================================================
#
# 1. Copy this file to .env (in your project root):
#    cp .env.example .env
#
# 2. Customize values for your environment
#
# 3. For local development with Docker Compose:
#    - Use default values (mock API)
#    - Run: docker-compose up -d
#    - Run tests: INTEGRATION_TESTS=true go test -v ./tests/integration/...
#
# 4. For testing against real dev API:
#    - Set INTEGRATION_TEST_MODE=dev
#    - Set INTEGRATION_TEST_API_URL to actual dev URL
#    - Set INTEGRATION_TEST_AUTH_TOKEN to JWT token
#
# 5. GitHub Actions secrets:
#    - Add sensitive values via GitHub Settings → Secrets
#    - Reference as ${{ secrets.SECRET_NAME }} in workflows
#    - Never commit real credentials to .env file
#
# ============================================================================
